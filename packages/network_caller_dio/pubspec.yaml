name: network_caller_dio
description: "Production-ready Dio networking for Flutter. Auto-handles tokens, refresh, interceptors, retry, and typed errors."
version: 1.0.1
homepage: https://github.com/Md-LatifurRatul/custom_network_caller
repository: https://github.com/Md-LatifurRatul/custom_network_caller/tree/main/packages/network_caller_dio
issue_tracker: https://github.com/Md-LatifurRatul/custom_network_caller/issues
topics:
  - networking
  - dio
  - api
  - rest
  - http-client

resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  network_caller_core: ^1.0.0
  dio: ">=5.9.0 <7.0.0"
  flutter_secure_storage: ">=9.2.4 <11.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ">=5.1.0 <7.0.0"
